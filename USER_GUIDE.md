# Light Up Random v1.0.0 User Guide

Light Up Random は、ブラウザだけで動作する HTML5 版の Light Up パズルアプリです。盤面は固定問題集ではなく、ユーザーが指定した条件に応じて毎回ランダム生成されます。生成された問題は、完全ソルバーによる一意解確認、数字ヒント削除、論理ソルバーによる仮置きなし判定、解き味評価、難易度評価を通して作られます。

This is a browser-based random Light Up puzzle app. It generates uniquely solvable puzzles locally in the browser and evaluates each generated puzzle with several internal metrics.

---

## 目次

- [このアプリでできること](#このアプリでできること)
- [ローカル処理とプライバシー](#ローカル処理とプライバシー)
- [対応ブラウザと推奨環境](#対応ブラウザと推奨環境)
- [Light Up パズルの基本ルール](#light-up-パズルの基本ルール)
- [基本ワークフロー](#基本ワークフロー)
- [画面構成](#画面構成)
- [盤面サイズ](#盤面サイズ)
- [生成条件](#生成条件)
- [マウス操作](#マウス操作)
- [状態表示](#状態表示)
- [シード](#シード)
- [詳細問題データ](#詳細問題データ)
- [ランダム生成ロジックの概要](#ランダム生成ロジックの概要)
- [完全ソルバーと一意解判定](#完全ソルバーと一意解判定)
- [数字削除ロジック](#数字削除ロジック)
- [仮置きなし判定と論理ソルバー](#仮置きなし判定と論理ソルバー)
- [解き味スコア](#解き味スコア)
- [難易度評価](#難易度評価)
- [only-cover について](#only-cover-について)
- [14 x 24 での上限付き削除固定](#14-x-24-での上限付き削除固定)
- [生成に時間がかかる場合](#生成に時間がかかる場合)
- [現在の制限事項](#現在の制限事項)
- [GitHub Pages で公開する場合](#github-pages-で公開する場合)
- [今後の拡張候補](#今後の拡張候補)

---

## このアプリでできること

このバージョンでは、次のことができます。

- Light Up パズルをランダム生成する
- 4種類の盤面サイズから選ぶ
- 仮置きなし問題だけを生成するか、仮置きが必要な問題も許容するかを選ぶ
- 数字ヒントを一意解の限界まで削るか、上限付きで削るかを選ぶ
- 左クリックで照明を置く / 外す
- 右クリックで「ここには置かない」メモの × 印を付ける / 外す
- 照明範囲を自動表示する
- 照明同士の衝突を赤く表示する
- 数字マスが満たされたときに緑枠で表示する
- 数字マスの照明数が超過したときに赤枠で表示する
- 未照明マス、照明衝突、数字超過を常時確認する
- 正解の照明位置を表示する
- 詳細問題データで、生成・評価・難易度関連の内部指標を確認する
- 日本語 / English のUI表示を切り替える

このアプリの中心は、単に問題を表示することではなく、**その場で問題を作ること**です。固定問題集を内蔵する方式ではなく、完全ソルバーと評価ロジックを使って、毎回条件に合う問題を生成します。

---

## ローカル処理とプライバシー

このアプリは、単体HTMLとしてブラウザ内で動作します。問題生成、ソルバー、一意解確認、論理ソルバー、評価値計算は、すべて利用者のブラウザ上で実行されます。

現行版では、問題データをサーバーに送信する機能はありません。GitHub Pages で公開した場合でも、配信されるのは HTML、CSS、JavaScript などの静的ファイルだけです。問題生成やプレイ操作は利用者側のブラウザ内で完結します。

現在の v1.0.0 では、セーブデータ機能や実績保存機能はまだ実装していません。今後実績機能を追加する場合でも、基本方針としてはブラウザ内保存またはJSONエクスポートによるローカル保存を想定しています。

---

## 対応ブラウザと推奨環境

推奨環境は、デスクトップ版の最新 Google Chrome または Microsoft Edge です。

Light Up パズルの描画そのものは通常のモダンブラウザで動作する可能性がありますが、このアプリは問題生成時に比較的重い計算を行います。特に大きな盤面、仮置きなし条件、数字削除を強く行う設定では、完全ソルバーや論理ソルバーの計算量が増えます。

スマートフォンでも操作できるように盤面スケーリングを行っていますが、問題生成の速度は端末性能に大きく依存します。大きい盤面で生成に時間がかかる場合は、10 x 10 や 10 x 18 から試してください。

---

## Light Up パズルの基本ルール

Light Upは、白マスに照明を置いて、すべての白マスを照らすパズルです。

ルールは次の通りです。

1. 白マスには照明を置けます。
2. 黒マスには照明を置けません。
3. 数字付き黒マスの数字は、その上下左右に隣接する照明の数を表します。
4. 照明は、上下左右の直線方向に、黒マスまたは外枠に当たるまで光を照らします。
5. 照明同士が、黒マスに遮られず同じ行または列で照らし合ってはいけません。
6. 最終的に、すべての白マスが照らされている必要があります。

このアプリでは、照明されている白マスは明るく表示されます。照明同士の衝突は赤く表示されます。数字マスは、条件を満たすと緑枠、照明数が多すぎると赤枠になります。

---

## 基本ワークフロー

通常の使い方は次の通りです。

1. 盤面サイズを選びます。
2. 仮置きを許容するかどうかを選びます。
3. 数字削除方式を選びます。
4. **新規作成** を押します。
5. 問題が生成されるまで待ちます。
6. 盤面上で照明を置いて解きます。
7. 必要に応じて右クリックで × 印メモを付けます。
8. すべての白マスが照らされ、照明衝突と数字超過がなくなればクリアです。

生成中は、条件に合う問題が作られるまでアプリが計算を続けます。カーソルは待機表示になり、ボタンやプルダウンは一時的に無効になります。

---

## 画面構成

### ヘッダー

画面上部には、アプリ名、簡単なルール説明、言語切替ボタンがあります。

### 盤面ペイン

左側には問題盤面が表示されます。盤面はウィンドウサイズと盤面サイズに応じて自動スケーリングされます。10 x 18 や 14 x 24 のような縦長盤面でも、基本的には盤面内にスクロールバーが出ないようにセルサイズを調整します。

### 操作ペイン

右側には、問題設定、操作ボタン、状態表示、シード欄、詳細問題データボタン、簡単な操作説明があります。

通常画面では、プレイ中に必要な情報だけを表示します。生成ロジックや評価値に関する細かい数値は、**詳細問題データ** ダイアログに移しています。

---

## 盤面サイズ

v1.0.0 では、次の4サイズを採用しています。

| 表示 | 内部サイズ | マス数 | 位置づけ |
|---|---:|---:|---|
| 10 x 10 / やさしい | rows=10, cols=10 | 100 | 小さめの標準盤面 |
| 10 x 18 / ふつう | rows=18, cols=10 | 180 | 縦長の中盤面 |
| 14 x 18 / やや難しい | rows=18, cols=14 | 252 | 大きめの縦長盤面 |
| 14 x 24 / 難しい | rows=24, cols=14 | 336 | 現行版の最大盤面 |

表示上の `10 x 18` は「横10、縦18」を意味します。内部的には rows=18、cols=10 として扱います。

正方形サイズを単純に増やすのではなく、縦長サイズを中心にしている理由は、紙のパズル本で使いやすい盤面感に近づけるためです。また、マス数が段階的に増えるようにして、難易度や実績設計に使いやすい構成にしています。

---

## 生成条件

### 仮置き

**許容しない** を選ぶと、論理ソルバーで最後まで解ける問題だけを採用します。この設定では、アプリ内部の基本手筋で完走できる問題が出ます。ただし、人間にとって簡単とは限りません。特に only-cover が多い問題では、仮置きは不要でも見つけにくい確定手が出る場合があります。

**許容する** を選ぶと、一意解であることを確認できれば、論理ソルバーで完走できない問題も採用対象になります。この設定では、先読みや仮置きが必要な問題が出ることがあります。

通常は **許容しない** を推奨します。

### 数字削除

**限界まで削除** は、数字ヒントをできるだけ削ります。具体的には、数字を1つ消しても一意解が保たれる場合は削除し、複数解になる場合は戻します。この処理を繰り返し、残った数字を単独で消すと一意解が壊れる状態に近づけます。

**上限付き削除** は、削除テスト数に上限を設けて、数字ヒントを削りすぎないようにするモードです。大きな盤面では、生成時間と解き味のバランスを取りやすくなります。

通常は **限界まで削除** を推奨します。ただし、14 x 24 ではこの設定は強制的に **上限付き削除** に切り替わります。

---

## マウス操作

| 操作 | 動作 |
|---|---|
| 白マスを左クリック | 照明を置く / 外す |
| 白マスを右クリック | × 印メモを付ける / 外す |
| 照明がある白マスを右クリック | 照明を消して × 印を付ける |
| × 印がある白マスを左クリック | × 印を消して照明を置く |
| 黒マスをクリック | 何もしない |

× 印は、プレイヤー用のメモです。解答判定、照明範囲、ソルバー、クリア判定には影響しません。

---

## 状態表示

通常画面には、次の情報が表示されます。

| 項目 | 意味 |
|---|---|
| 難易度評価 | この問題の推定難易度です。Lv.1〜Lv.5 相当の帯で表示されます。 |
| 解き味 | 問題の解き味を示す観察用スコアです。 |
| 未照明 | まだ照らされていない白マス数です。 |
| 照明衝突 | 他の照明を照らしてしまっている照明数です。 |
| 数字超過 | 数字付き黒マスの隣接照明数が数字を超えている箇所数です。 |
| クリア | 現在の盤面が完成しているかを表示します。 |

**仮置きなしで解けるかどうか** は、通常画面には表示しません。これは、問題を解く前に「この問題は仮置き不要である」と分かること自体がヒントになる可能性があるためです。必要な場合は、**詳細問題データ** を開いて確認できます。

---

## シード

シードは、ランダム生成の元になる文字列です。

現在表示されているシードを控えておくと、同じバージョン、同じ生成条件、同じサイズで問題を再現しやすくなります。ただし、生成ロジックを将来変更した場合、同じシードでも別の問題になる可能性があります。

公開版や長期保存で完全に同じ問題を保持したい場合は、シードだけではなく、問題盤面そのものを保存する形式が必要です。現行版では、シードは主に開発・確認・再現用の補助情報として扱います。

---

## 詳細問題データ

**詳細問題データ** ボタンを押すと、生成・評価に関する内部データを確認できます。

通常プレイでは不要ですが、問題の解き味や生成ロジックを確認したい場合に使います。各パラメータには、マウスオーバーで簡単な説明が表示されます。

主なセクションは次の通りです。

- 総合・生成情報
- 数字削除
- 難易度評価
- 論理ソルバー進行
- 手筋内訳
- 盤面構造
- 数字分布
- 暫定メモ

詳細問題データは、問題を解くためのヒントになる場合があります。純粋に解きたい場合は、クリア後に確認することを推奨します。

---

## ランダム生成ロジックの概要

このアプリの問題生成は、単に黒マスや数字をランダムに置く方式ではありません。ランダム配置だけでは、解なし、複数解、または解き味の悪い問題が大量に発生するためです。

基本的な流れは次の通りです。

```text
1. 黒マス配置をランダムに作る
2. その黒マス配置に対して、照明の完成解を作る
3. 完成解から、各黒マスの周囲にある照明数を計算する
4. 黒マスに数字ヒントを付ける
5. 完全ソルバーで一意解かどうかを確認する
6. 数字削除モードに応じて、数字ヒントを削る
7. 削除後も一意解が保たれるか確認する
8. 仮置き条件に応じて、論理ソルバーで解けるか確認する
9. 解き味スコアと難易度評価を計算する
10. 条件を満たした問題を出題する
```

この方式は、**完成解先行型** の生成です。先に答えを持つ盤面を作り、そこから問題として成立するように数字ヒントを調整します。

---

## 完全ソルバーと一意解判定

完全ソルバーは、問題が本当に一意解かどうかを確認するためのバックトラックソルバーです。

各白マスについて、照明を置くか置かないかを候補として扱います。ただし、全探索をそのまま行うと非常に重いため、次のような枝刈りを行います。

- 照明同士が見えてしまう場合は不正
- 数字付き黒マスの照明数が数字を超えたら不正
- 数字付き黒マスが、残り候補を全部使っても数字に届かない場合は不正
- 未照明白マスを照らせる候補が残っていない場合は不正
- 数字マスが満たされた場合は周囲の残り候補を禁止
- 数字マスの残り必要数と残り候補数が一致した場合は、すべて照明にする
- 白マスを照らせる候補が1つしかない場合は、その候補を照明にする

完全ソルバーは、解を2つ見つけた時点で打ち切ります。つまり、生成時に必要なのは「すべての解を列挙すること」ではなく、次の3分類です。

```text
0解     : 問題として不成立
1解     : 一意解
2解以上 : 複数解
```

このアプリでは、一意解でない問題は出題しません。

---

## 数字削除ロジック

完成解から数字を付けた直後は、数字ヒントが多めに残ります。この状態では解きやすい一方で、人間が作る良問に比べると冗長に見えることがあります。

そのため、数字削除処理を行います。

### 限界まで削除

限界削除では、数字ヒントを1つずつ削って確認します。

```text
1. 数字ヒントの位置をランダム順に並べる
2. ある数字を消す
3. 完全ソルバーで一意解か確認する
4. 一意解なら、その数字は削除したままにする
5. 複数解または解なしなら、その数字を戻す
6. 残った数字をもう一度削れるか試す
7. これ以上単独で削れる数字がなくなるまで繰り返す
```

これは、厳密な意味で「全可能性の中で最少数字数」を保証するものではありません。削除順に依存するローカル最小です。しかし、冗長な数字をかなり減らす効果があります。

### 上限付き削除

上限付き削除では、削除テスト回数に上限を設けます。大きな盤面では、限界削除に時間がかかりすぎることがあります。また、数字を削りすぎると、数字マス由来の局所的な解き筋が減り、only-cover 依存の候補探索問題になりやすい場合があります。

上限付き削除は、そのような大盤面での速度と解き味のバランスを取るためのモードです。

---

## 仮置きなし判定と論理ソルバー

このアプリでは、完全ソルバーとは別に、人間向けの基本手筋だけを使う論理ソルバーを持っています。

完全ソルバーは、一意解を保証するためのものです。バックトラックを使うため、人間が自然に解けるかどうかは分かりません。

論理ソルバーは、仮置きを使わずに、次のような基本手筋でどこまで進むかを確認します。

### 数字満了 → 禁止

数字付き黒マスの周囲に、すでに必要数の照明が置かれている場合、その周囲の残り候補には照明を置けません。

例：1 の周囲にすでに1個の照明がある場合、残りの隣接白マスは照明不可になります。

### 数字不足分すべて照明

数字付き黒マスで、必要数と残り候補数が一致する場合、その残り候補はすべて照明になります。

例：3 の周囲で、照明候補がちょうど3か所しか残っていない場合、その3か所はすべて照明です。

### 照明直線 → 禁止

照明を置いた場合、その照明から上下左右に見える白マスには、別の照明を置けません。

### 唯一照明候補（only-cover）

ある白マスは必ず照らされる必要があります。その白マスを照らせる候補が1か所しか残っていない場合、その候補は照明になります。

この手筋はLight Upの正式な論理として正しいものです。ただし、数字マスを直接見る手筋よりも、候補探索感が強くなります。そのため、解き味スコアでは only-cover の量や重さを評価しています。

---

## 解き味スコア

解き味スコアは、問題が「気持ちよく解けそうか」を見る観察用スコアです。

これは数学的な正しさではなく、パズルとしての感触を推定するための値です。主に減点式で計算します。

主な評価要素は次の通りです。

- 入口が少なすぎないか
- 1手ずつしか進まない局面が長く続かないか
- only-cover に依存しすぎていないか
- 重い only-cover が多すぎないか
- 手筋が単調すぎないか
- 数字ヒントが少なすぎないか
- 0 や 4 のような強いヒントに偏りすぎていないか
- 長い白マス通路が多すぎないか
- 黒マスや数字ヒントが盤面の一部に偏りすぎていないか

スコアが高いほど、アプリ内部では良問に近いと見なします。ただし、これはあくまで推定です。人間が実際に解いた感触と完全に一致するものではありません。

---

## 難易度評価

難易度評価は、解き味スコアとは別の指標です。

解き味スコアは「良問らしさ」を見ます。難易度評価は「解くときの認知負荷」を見ます。

主な評価要素は次の通りです。

- 論理ラウンド数
- 主手筋数
- 入口手数
- 1手進行の連続
- 最大手筋難度
- 平均手筋難度
- only-cover 比率
- 重い only-cover の数
- 長い白マス通路数
- 数字密度の低さ

難易度は Lv.1〜Lv.5 相当の帯で表示されます。これは公式な難易度ではなく、このアプリ独自の推定値です。

同じ難易度でも、解き味が良い問題と悪い問題があります。逆に、難しくても面白い問題もあります。そのため、このアプリでは **解き味** と **難易度** を分けて表示しています。

---

## only-cover について

only-cover は、このアプリの評価ロジックで特に重要な手筋です。

only-cover とは、ある白マスを照らせる候補が1か所しか残っていないため、そこに照明を置くしかない、という手筋です。

この手筋は論理的には正しいですが、人間にとっては見つけにくい場合があります。

数字マス由来の手筋は、目線の起点が明確です。

```text
この 0 の周囲には置けない
この 3 は残り全部置く必要がある
この 1 はもう満たされた
```

一方、only-cover は、白マス側から考えます。

```text
この白マスは照らされなければならない
この白マスを照らせる候補を全方向に探す
残っている候補が1か所しかない
```

そのため、only-cover が多すぎる問題は、仮置きなしで解けるとしても、候補探索が重く感じられます。

このアプリでは、only-cover をさらに細かく評価します。

| 指標 | 意味 |
|---|---|
| only-cover比率 | 全手筋に占める only-cover の割合 |
| 入口only-cover | 初期局面から出る only-cover の数 |
| only-cover連続 | only-cover が連続する最大回数 |
| 重いonly-cover | 長い視線確認や遠い候補探索を伴う only-cover |
| 広域only-cover | 確認範囲が広い only-cover |
| 長距離only-cover | 長い行・列を見通す必要がある only-cover |
| 数字非隣接only-cover | 数字マスに隣接しない白マス起点の only-cover |

解き味スコアでは、こうした重い only-cover を減点しています。

---

## 14 x 24 での上限付き削除固定

14 x 24 は、現行版の最大盤面です。

このサイズで、仮置きを許容せず、数字を限界まで削除すると、生成に30秒以上かかることがあります。また、生成された問題も数字ヒントが少なすぎ、解き味が候補探索寄りになりやすい傾向があります。

そのため、v1.0.0 では、14 x 24 を選んだ場合、数字削除は強制的に **上限付き削除** に切り替わります。プルダウン上でも選択肢は固定されます。

これは、生成時間と解き味のバランスを取るための仕様です。

---

## 生成に時間がかかる場合

問題生成は、条件を満たすまで続きます。

特に時間がかかりやすい条件は次の通りです。

- 大きい盤面を選ぶ
- 仮置きを許容しない
- 数字削除を限界まで行う
- 一意解確認が難しい黒マス配置が続く
- 論理ソルバーで完走できる候補がなかなか出ない

生成中は、画面上部に生成中メッセージが表示され、カーソルは待機状態になります。ボタンやプルダウンは一時的に無効化されます。

生成が重い場合は、次の設定を試してください。

```text
・サイズを小さくする
・数字削除を上限付きにする
・仮置きを許容する
```

---

## 現在の制限事項

v1.0.0 には、次の制限があります。

- セーブデータ機能は未実装です。
- 実績機能は未実装です。
- 問題集作成機能は未実装です。
- シードは完全な長期再現保証ではありません。
- 難易度評価と解き味スコアは推定値です。
- 論理ソルバーはすべての人間的手筋を網羅しているわけではありません。
- 14 x 24 では数字削除が上限付きに固定されます。
- 生成処理はメインスレッドで実行されるため、生成中は一時的にUIが固まる場合があります。

今後、大きな盤面や問題集生成を本格化する場合は、Web Worker による生成処理の分離が有効です。

---

## GitHub Pages で公開する場合

このアプリは、単体HTMLとして公開できます。

GitHub Pages で公開する場合、基本的には次のファイルを配置すれば動作します。

```text
index.html
```

配布パッケージでは、GitHub Pages 向けに `index.html` として同梱しています。

GitHub Pages 上でも、問題生成やプレイ処理は利用者のブラウザ内で行われます。サーバー側で問題を生成する仕組みではありません。

---

## 今後の拡張候補

今後の拡張としては、次の方向が考えられます。

### 実績機能

ランダム問題を解くアプリとしては、問題集よりも実績機能との相性がよいです。

例：

- 総クリア数
- サイズ別クリア数
- 難易度別クリア数
- 10 x 18 Lv.3 クリア
- 14 x 24 クリア
- 仮置き許容問題クリア
- 仮置きなし問題クリア

### セーブ / ロード

実績や現在の途中盤面をJSONとして保存し、あとで復元できるようにします。

保存対象の候補は次の通りです。

```text
・現在の問題盤面
・正解データ
・現在の照明配置
・×印メモ
・クリア統計
・実績解除状態
・表示言語
・直近の生成条件
```

### Web Worker 化

問題生成を Web Worker に移すことで、生成中もUIを固めずに進捗表示できます。大きい盤面や条件の厳しい生成では特に有効です。

### 問題集モード

現行方針ではランダム1問モードを中心にしていますが、条件を指定して50問程度の個人用問題集を作るモードも追加可能です。

### 追加サイズ

現在は 14 x 24 を上限としています。さらに大きなサイズを試すことは可能ですが、生成時間、セルサイズ、視認性、only-cover偏重の問題が大きくなります。

---

## ライセンス

このアプリは MIT License で配布します。詳細は同梱の `LICENSE` を参照してください。

---

## まとめ

Light Up Random v1.0.0 は、シンプルなクリック操作で遊べる一方、内部ではかなり重い生成・検証ロジックを持つランダムパズルアプリです.

設計上の中心は、次の4点です。

```text
1. 完全ソルバーで一意解を保証する
2. 数字ヒントを削って冗長さを減らす
3. 論理ソルバーで仮置きなし問題を判定する
4. 解き味と難易度を別軸で評価する
```

このため、単に「ランダムに黒マスと数字を置いた問題」ではなく、問題として成立し、ある程度の解き味を持つ問題をその場で作れることを目指しています。
